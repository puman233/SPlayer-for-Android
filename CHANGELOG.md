# 更新日志

所有版本的更新记录。最新版本在最上方。

---

## v3.0.8

### 🖥 桌面歌词控件全面重构（复刻桌面端 SPlayer）
- **设备差异化适配**：
  - **平板**：完整复刻桌面端 header 三栏布局（音符+歌名｜上一首/播放暂停/下一首｜红心/锁定/关闭），默认位于垂直屏幕 **70%** 处，窗口高度自动加高以容纳 header 与歌词
  - **手机**：保留播放/暂停、上一首、下一首、锁定、关闭 5 个控制按钮，默认位于垂直屏幕 **30%** 处，宽度为屏幕 **84%**（左右各距边缘 8%）
- **单色线稿图标**：将按钮图形由 emoji 全部替换为**单色线稿矢量图标**（音符、上一首/下一首、播放/暂停、关闭、锁定/解锁、红心），复刻桌面端 SPlayer 视觉风格
- **红心收藏按钮**：原「设置/齿轮」改为**红心**按钮，点击可「添加/取消收藏」当前歌曲；已收藏显示实心红心，未收藏显示描边红心
- **沉浸式交互**：未触碰时仅显示歌词，控制栏自动隐藏；点击后显示控制栏，数秒无操作自动隐藏
- **智能自适应**：歌词字号按窗口高度与屏幕尺寸智能调整；控件拖动与初始位置按屏幕边界钳制，确保不越出屏幕、按钮可点
- **锁定**：锁定后位置固定、无法拖动/关闭，显示独立解锁按钮；保留 AMLL 逐字渐变渲染、双行、行切换动画

### 🎨 UI 修复
- 🐛 **修复手机播放页底部三个点重叠**：调整歌曲信息区标签与操作按钮布局（flex-wrap：nowrap），并加大歌单计数徽标与「更多」按钮的间距，避免重叠
- 🐛 **修正次级菜单阴影逻辑**：右上角次级菜单（设置/账号）文字**不再加阴影**；**仅歌曲播放页三点快捷菜单**文字保留阴影；弹层面板投影保留
- 🐛 **修复点击红心收藏时报错乱码**：修正收藏相关提示文案的编码错误（如「请先登录后再收藏歌曲」「收藏操作失败，请重试」），消除乱码

### 🔄 软件热重载
- 用户**登录成功、退出登录、切换账号**后自动热重载 WebView，UI 立即刷新为对应登录状态

---
## v3.0.7

### ✨ 新功能

- 🔄 **软件热重载**：右上角齿轮 → 设置菜单新增「软件热重载」，Android 端点击后重载 WebView 内容（不重启 App、尽量保留播放状态），Electron 端仍走 IPC 刷新窗口
- 📱 **底部导航长按刷新**：推荐/发现/收藏/最近四个 Tab 长按 2 秒刷新当前页面数据与视图；图标按压缩放 0.92、弹起回弹 1.0（cubic-bezier(0.34,1.56,0.64,1) 弹性曲线、300ms），不干扰短按切换
- 🎧 **歌词页播放控制**：歌词页底部新增播放控制模块（上一首/播放暂停/下一首、随机、循环、进度条），样式、颜色与播放页控制栏一致；无操作 3 秒渐隐（透明度 1→0、耗时 1s），触摸屏幕重新渐显，显示时为被遮挡歌词预留空间
- 🖥 **全局设置全屏化**：点击「全局设置」后整页全屏展开，左侧分类导航 + 右侧具体设置项，保留全部原有设置项

### 🛠 调整与修复

- **关于软件 / 问题反馈**：检查更新与最新/历史版本改为读取 puman233/SPlayer-for-Android 仓库的 GitHub Release；问题反馈跳转安卓端仓库 Issue 页面并在描述中预填设备信息
- **移除原项目信息**：关于页删除「加入 QQ 交流群」「开发人员」「更多贡献者」「社区与资讯」区块及其相关代码
- 🎨 **歌词页/播放页布局统一**：信息页控制卡底部锚定，与歌词页控制模块位置/高度一致，左右滑动切换更流畅；并预留底部空间避免与翻页圆点重叠
- 🎤 **桌面歌词即时显示与默认位置**：开启后立即同步当前歌曲信息/歌词/进度/配置；默认悬浮窗宽度为屏幕 84%，默认位置手机 30%、平板 70%
- 🔍 **检查更新改为应用内**：不再跳转外部，应用内查询最新版本并提示「发现新版本 / 已是最新版本」

### 🔧 修复

- 🐛 **修复全屏设置页无法滚动**：移除滚动容器过度的 overflow:hidden 并约束滚动高度，设置项可正常下滑
- 🎨 **提升弹层菜单可读性**：为下拉/弹层菜单文字增加阴影，避免与模糊/动态封面背景混淆
- ⚡ **底部导航长按刷新响应优化**：触发时间由 2 秒缩短至 700ms，并在触发时震动以提供即时反馈

---

## v3.0.6

### 🎨 UI 布局修复与自适应增强

- 🐛 **修复右下角浮层按钮遮挡播放栏**：「我喜欢的音乐」等列表页下滑时，「定位歌曲/回到顶部」浮层按钮会遮挡底部浮岛播放栏。已将播放栏层级提升（`z-index` 高于列表浮层），并将浮层按钮上移至播放栏与底部导航之上，滚动时自动淡出
- 🐛 **修复歌手详情页文字错位**：歌手名较长换行时，「歌曲/专辑/视频」信息与「播放/关注」按钮重叠。已将移动端歌手详情改为歌手名单行 + 绝对定位稳定布局，信息与按钮不再互相覆盖
- 📐 **全局自适应优化**：列表页浮层按钮、详情页头部等采用相对单位与固定安全区计算，适配不同分辨率真机

### ✨ 新功能

- 🔄 **热重载**：设置 → 常规设置 → 其他设置新增「热重载」按钮，开发调试时可快速重载界面（保留播放列表等本地数据）
- 🔔 **通知栏控制权限按需申请**：开启「通知栏音乐控制器」时，先弹出 App 风格确认框，确认后申请系统通知权限（Android 13+）；拒绝则提示功能不可用。授权后通知栏显示播放/暂停/上一首/下一首等控制按钮
- 🎤 **桌面歌词（悬浮歌词）恢复**：开启桌面歌词时按需申请悬浮窗权限，跳转系统设置授权后显示屏幕顶层滚动歌词；未授权时禁用并提示
- ❤️ **心动模式按钮**：「我喜欢的音乐」页播放按钮旁新增红心按钮，一键开启心动模式（随机播放心动推荐）

### 🔒 权限说明

- 恢复 `POST_NOTIFICATIONS` 与 `SYSTEM_ALERT_WINDOW` 权限声明；权限均改为**用户主动启用对应功能时才申请**，启动与日常使用不再打扰
- 弹窗遵循「先 App 自定义确认，再调系统授权」原则，样式与主题一致

---

## v3.0.5

### ⚡ 性能优化与权限精简

- 🚀 **慢请求诊断**：请求层新增耗时统计，超过 2 秒的请求自动输出 `[perf]` 日志，便于排查页面加载/内容刷新卡顿
- 📦 **构建分包优化**：核心框架（vue/vue-router/pinia/@vueuse）、Naive UI、axios 独立分包，减少首屏解析量并提升资源缓存复用
- 🔇 **移除通知权限**：不再请求 `POST_NOTIFICATIONS`，启动与开启媒体控制器时均不再弹出系统权限请求（媒体播放不受影响）
- 🚫 **移除悬浮窗权限**：不再请求 `SYSTEM_ALERT_WINDOW`，Android 端悬浮歌词功能同步下线（桌面端不受影响）
- ⚖️ **补充开源声明**：README 与「关于」页新增免责声明（基于 SPlayer/SPlayer-Next 二开、AGPL-3.0、仅供学习交流）

---

## v3.0.4

### 🔓 修复解锁请求路径错误（VIP 歌曲无法播放）

- 🐛 **根因**：前端 `unlockSongUrl` 固定使用 `baseURL: "/api/unblock"`（桌面版路由），但 Android 端 baseURL 会被自动覆盖为嵌入式服务器地址（含 `/api/netease` 前缀），导致解锁请求实际发往 `/api/netease/netease` → 服务器 404 → 解锁失败 → 所有 VIP 歌曲报「暂无权限播放，已跳过」
- 🛠 **修复**：`unlockSongUrl` 按平台动态构造路径——Android 请求 `/api/netease/unblock/{server}`（匹配嵌入式服务器路由），桌面版保持 `/api/unblock/{server}` 不变
- ✅ **真机验证**：VIP 歌曲《YELLOW》（神山羊，2:58）完整播放，netease 源返回完整 FLAC（SQ 无损），播放进度正常走完

---

## v3.0.3

### 📦 体积与日志优化

- 📉 **APK 体积瘦身**：构建时排除网易 API vendor 中运行时用不到的文档/静态资源（`public` 目录，约 12.8MB），APK 由 **73.8MB → 61.4MB**（减 17%）
- 📝 **统一解锁日志前缀**：服务器端 `API/unblock/*` 所有日志统一加 `[unblock]` 前缀，logcat 过滤更便捷（`adb logcat | grep "\[unblock\]"`）
- 🔧 修正构建顺序：`cap sync` 前清空 `dist/capacitor/nodejs-project`，避免 nodejs-project 被重复打包进 `public` 导致 APK 体积膨胀（曾膨胀至 92.9MB）
- ✅ 瘦身后功能完整验证：搜索 / song/url / 三源解锁（netease/kuwo/bodian）全部正常，真机实测解锁返回完整 FLAC/mp3

---

## v3.0.2

### 🎵 修复 VIP 歌曲 30 秒试听问题

- 🎯 **定位根因**：官方接口对 VIP 歌曲返回 30 秒试听 URL（`freeTrialInfo` 0~35s），此前 `getAudioSource` 在开启「允许播放试听」时**直接返回官方试听、跳过解锁**，导致只播 30 秒
- 🔓 **解锁优先**：官方为试听时**始终优先触发多源解锁**（网易/酷我/波点并发），解锁成功即返回完整直链；仅当解锁失败且允许试听时才回退试听片段
- 🧹 **缓存防污染**：试听 URL 一律不写入本地缓存（此前开启试听播放时会缓存试听链接，二次播放命中试听缓存仍只有 30 秒）
- 🛡️ **试听链接防御检测**：解锁返回的 URL 若含 `preview`/`试听`/`freetrial` 等试听标记，视为解锁失败并跳过该源，继续尝试下一音源
- 📝 **解锁链路日志**：每个音源返回结果（状态码 + URL）输出至 logcat，便于真机排查
- ✅ 实测验证：测试曲《ツギハギスタッカート》《YELLOW》官方均为试听，解锁后返回完整 FLAC（20~26MB）/ mp3 完整版，可完整播放

---

## v3.0.1

### 🔓 网易（netease）音源修复

- 🩺 **深度诊断确认**：netease 源（GD音乐台）本身工作正常，对网易云有资源的歌曲稳定返回可播放直链（实测《夜曲》《起风了》返回 FLAC，450~970ms）；此前《晴天》等返回空，是**网易云已下架该歌曲版权**所致，非代码缺陷
- ⏱️ 为 netease 请求增加 **10s 独立超时** — GD音乐台偶尔限流/超时时不再拖慢整条解锁链路
- 📡 增加 **UA / Referer 请求头**，提升镜像源兼容性
- 🛡️ 增强**响应结构校验** — 仅接受含 `url` 字段的对象，异常数据安全降级
- 📝 完善解锁日志 — 输出请求状态、完整响应数据；空 URL 时明确提示「歌曲可能已下架或无版权，将降级其他音源」，便于 logcat 排查
- 🔀 确认**降级链路完整** — 网易下架歌曲（如《晴天》）netease 返回 404 后，自动由酷我/波点接管（实测均返回可播放 mp3）

---

## v3.0.0

### 🔓 歌曲解锁（VIP 播放）

- 🎯 移植 **SPlayer 桌面版完整解锁机制** — 官方链接失效或仅试听时，自动多源并发解锁
- 🔀 音源优先级：**网易(GD音乐台) → 酷我 → 波点 → 歌曲宝**，全部音源并行请求，按配置顺序取首个成功源
- ⏱️ 新增 **单源请求超时**（8s）— 慢速/失效音源不再阻塞播放，彻底解决解锁卡顿
- 🚫 默认关闭已失效的 **歌曲宝** 源，可在「音源配置」中手动开启
- 🔗 解锁 URL 缓存至本地，再次播放秒开；**试听片段不写入缓存**，避免污染本地缓存
- 🎧 解锁失败时**静默回退官方试听**，不报错、不弹窗，播放不中断
- 🔄 支持 **手动换源** — 播放页切换指定音源重试解锁
- 📝 解锁全链路日志（触发/成功/失败/回退）输出至 logcat，便于真机排查

### 📡 嵌入式服务器

- 🔧 修复 **127.0.0.1:1145 连接问题** — 嵌入式 Node 服务器启动、就绪检测、自动重启全链路加固
- 🛣️ 新增 **`/api/netease/unblock/{server}`** 解锁路由，与前端 `unlockSongUrl` 契约对齐
- 🧩 移植 `kuwo`（kwDES 加密）、`bodian`（酷我系签名+广告免费）、`netease`（GD音乐台）、`gequbao` 四源实现
- 🚀 服务器打包体积优化（esbuild minify），启动更快
- 🛡️ 服务器异常捕获与自动重试，崩溃后自动恢复

### ⚡ 播放与性能

- ⚡ 切歌前**预下载下一首音频前 512KB**，ExoPlayer 秒开无断音
- 🚀 优化冷启动，嵌入式 API 就绪等待与轮询兜底，减少首屏白屏
- 💾 Android 端 URL 缓存 + 本地缓存兜底，弱网环境仍可播放
- 🧹 请求失败自动重启嵌入式 API 并重试，网络波动自动恢复

---

## v3.0.0-rc.4

### 🎵 播放器

- 🎴 新增 **移动端卡片式播放器** — 支持动画过渡与手势交互（上滑展开/下滑收起）
- 🎛️ 新增 **播放器快捷操作菜单** — 一键切换音质、收藏、下载等常用操作
- 🎚️ 重构 **播放系统** — 支持移动端音频频谱可视化与原生播放队列管理
- 📈 优化 **进度条** — 悬浮信息提示、触摸事件处理更精准
- 🔧 修复拖拽进度后 seek 位置丢失、切歌进度条跳变等问题
- ⚡ 优化 **冷启动速度** 与渲染性能，减少首屏白屏时间
- 🔄 提取播放信息推断逻辑，代码结构更清晰

### 📱 移动端 / 平板体验

- 🖼️ 新增 **页面磨砂效果** — 部分界面背景采用高斯模糊，视觉层次更丰富
- 📱 新增 **手机横屏沉浸式播放模式** — 全屏封面 + 歌词，无干扰聆听
- 🧩 新增 **评论页嵌入竖屏页面** — 播放页内直接查看评论，无需跳转
- ↔️ 重构 **页面缩放逻辑** — 适配手机 / 平板 / 折叠屏等多设备缩放场景
- 🏠 完善 **移动端竖屏适配** — SongList、本地页列表/歌手/专辑/文件夹全面优化
- ⬅️ 新增 **Android 全局返回键处理栈** — 弹窗/菜单/登录页统一返回键拦截，退出前清理后台服务

### 🎸 本地音乐 & 缓存

- 🎵 新增 **本地歌曲封面提取** — 自动从音频文件读取内嵌封面
- 🔍 修复 **文件夹路径解析** 异常，本地音乐扫描更稳定
- 🧹 新增 **播放缓存清理接口**，管理缓存更灵活
- ⚡ 实现 **本地缓存与预加载优化** — 播放更流畅，减少网络请求
- 🖼️ 适配 **Android 原生 file/content 协议图片加载**，本地封面正常显示

### 🎤 歌词

- 🎯 修复 **AMLL 歌词** 水平内边距绑定、头部对齐、padding 偏移等问题
- ⬆️ 更新 **AMLL 依赖** 到最新稳定版
- 🔧 更新 AMLL 默认配置项参数
- 🎨 修复纯净模式居中间歇点选择器匹配范围

### ⚙️ 设置

- 🗂️ 重新整理 **Android 端设置分组** — 将 Android 专用项（通知栏控制器、允许混音、状态栏/导航栏等）独立为「Android 系统设置」分组
- 💾 新增 **设置导出** — 通过 SAF 目录选择器导出全部设置为 JSON 文件，支持备份迁移
- 📥 新增 **设置导入** — 选取之前导出的 JSON 配置文件一键恢复
- 🖱️ Android 端隐藏不适用的「右键菜单」设置项

### ⚙️ 其他

- 🎨 优化 **主题配色** 与磨砂背景默认参数（模糊值 4px）
- 🔧 修复 **播放器状态同步** — 修正 Android 原生播放器时间单位，避免重复触发 ENDED
- 📦 新增 `@capacitor/screen-orientation` 依赖，支撑横屏模式
- 📝 PR 模板目标分支从 master 改为 dev

---

## v3.0.0-rc.3

- ☁️ 新增 **WebDAV 音乐源** — 支持通过 WebDAV 连接并播放远程音乐
- 🎵 新增 **本地歌曲** — 扫描本地音乐文件，与在线曲库统一管理
- 🔍 新增 **页面缩放** — 50%–150% 全局缩放，缩小后等效视口变宽，达到阈值可切换到 Pad 布局（尚未完全适配，欢迎 PR 帮忙完善；没有 PR 的话也尽量在下个版本彻底做好）
- 📂 新增 **本地 TTML/LRC 歌词支持** — 歌词目录扫描支持 `.lrc` 和 `.ttml`，按文件名 ID 和元数据匹配
- 🎵 新增 **同目录歌词查找** — 播放本地音乐时自动查找同目录下的歌词文件
- 🔗 新增 **网络代理设置** — Android 端可配置 HTTP/HTTPS 代理用于内置 API 请求
- ⬇️ 新增 **音乐下载** — 开发者模式下可通过 SAF 选择下载目录，下载歌曲及歌词
- 🌐 完善 **流媒体支持** — Navidrome / Jellyfin / Emby / Subsonic / OpenSubsonic 全平台可用
- 📱 最低兼容性调整为 **Android 10**
- 📐 重做 **手机端底栏 / 播放栏布局** — 底栏改成全宽贴底实心条，播放栏改为圆角浮岛悬浮其上；底栏选中高亮改为基于 JS 测量的**滑动指示器**，切换页签时平滑过渡而非瞬移
- 🎯 手机端整体 **轻度紧凑（约 -15%）** — 收紧 CoverList 卡片间距与文字区 padding/字号、SongCard 内边距与 cover 尺寸、ListDetail 头部高度、Setting 项间距与卡片 padding；平板（shortestSide ≥ 768）保持原样
- 🔧 修复 **显示系统状态栏** 滑动后自动隐藏的问题
- 🔧 修复 **允许与其他应用同时播放** 无效的问题 — 开启时改用 ExoPlayer 引擎（handleAudioFocus=false）
- 🔧 修复 **Android 触控跳转** — 修复触控进度跳转异常
- 🔧 修复 **跳转后 seek 位置丢失回到 0:00**
- 🔧 修复 **自动切下一首进度条跳到非 0:00 错误位置**
- 🎚️ 新增 **全局歌词偏移设置**（PR #11）

---

## v3.0.0-rc.2

- 🔄 修复 **横竖屏切换** 时部分界面布局错乱
- 📱 修复 **手机小屏** 下文字挤压成一团的问题
- ⬆️ 调整 **"回到顶部"按钮** 位置，不再遮挡其他控件
- 🎨 修复 **桌面歌词** 在未开启文本背景遮罩时、锁定状态下仍显示背景的 bug
- 📦 GitHub Actions 构建改为 **分架构打包**，单包体积更小

---

……
